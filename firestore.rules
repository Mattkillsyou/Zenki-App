rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test mode — open read/write for development.
    // TODO: tighten before production launch.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 5, 16);
    }
  }
}
